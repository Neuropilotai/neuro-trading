'use strict';

/** Stub for v2 minimal runnable. Replace with full implementation when adding universe config. */
async function load() {
  return;
}

function getSymbolTimeframePairs() {
  return [];
}

module.exports = {
  load,
  getSymbolTimeframePairs,
};
